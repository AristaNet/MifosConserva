import { async, ComponentFixture, TestBed } from '@angular/core/testing';

import { SeasonalitySaleComponent } from './seasonality-sale.component';

describe('SeasonalitySaleComponent', () => {
  let component: SeasonalitySaleComponent;
  let fixture: ComponentFixture<SeasonalitySaleComponent>;

  /* beforeEach(async(() => {
    TestBed.configureTestingModule({
      declarations: [ SeasonalitySaleComponent ]
    })
    .compileComponents();
  }));

  beforeEach(() => {
    fixture = TestBed.createComponent(SeasonalitySaleComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  }); */
});
